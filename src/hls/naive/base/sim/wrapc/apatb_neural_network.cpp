// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.4
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// ==============================================================

#define AP_INT_MAX_W 32678

#include <systemc>
#include <iostream>
#include <cstdlib>
#include <cstddef>
#include <stdint.h>
#include "SysCFileHandler.h"
#include "ap_int.h"
#include "ap_fixed.h"
#include <complex>
#include <stdbool.h>
#include "autopilot_cbe.h"
#include "ap_stream.h"
#include "hls_stream.h"
#include "hls_half.h"
#include "hls_signal_handler.h"

using namespace std;
using namespace sc_core;
using namespace sc_dt;


// [dump_struct_tree [build_nameSpaceTree] dumpedStructList] ---------->


// [dump_enumeration [get_enumeration_list]] ---------->


// wrapc file define: "mem_V"
#define AUTOTB_TVIN_mem_V  "../tv/cdatafile/c.neural_network.autotvin_mem_V.dat"
// wrapc file define: "out_V"
#define AUTOTB_TVOUT_out_V  "../tv/cdatafile/c.neural_network.autotvout_out_V.dat"
#define AUTOTB_TVIN_out_V  "../tv/cdatafile/c.neural_network.autotvin_out_V.dat"
// wrapc file define: "w1_offset"
#define AUTOTB_TVIN_w1_offset  "../tv/cdatafile/c.neural_network.autotvin_w1_offset.dat"
// wrapc file define: "w2_offset"
#define AUTOTB_TVIN_w2_offset  "../tv/cdatafile/c.neural_network.autotvin_w2_offset.dat"
// wrapc file define: "input_offset"
#define AUTOTB_TVIN_input_offset  "../tv/cdatafile/c.neural_network.autotvin_input_offset.dat"
// wrapc file define: "b1_offset"
#define AUTOTB_TVIN_b1_offset  "../tv/cdatafile/c.neural_network.autotvin_b1_offset.dat"
// wrapc file define: "b2_offset"
#define AUTOTB_TVIN_b2_offset  "../tv/cdatafile/c.neural_network.autotvin_b2_offset.dat"
// wrapc file define: "result_offset"
#define AUTOTB_TVIN_result_offset  "../tv/cdatafile/c.neural_network.autotvin_result_offset.dat"

#define INTER_TCL  "../tv/cdatafile/ref.tcl"

// tvout file define: "out_V"
#define AUTOTB_TVOUT_PC_out_V  "../tv/rtldatafile/rtl.neural_network.autotvout_out_V.dat"

class INTER_TCL_FILE {
	public:
		INTER_TCL_FILE(const char* name) {
			mName = name;
			mem_V_depth = 0;
			out_V_depth = 0;
			w1_offset_depth = 0;
			w2_offset_depth = 0;
			input_offset_depth = 0;
			b1_offset_depth = 0;
			b2_offset_depth = 0;
			result_offset_depth = 0;
			trans_num =0;
		}

		~INTER_TCL_FILE() {
			mFile.open(mName);
			if (!mFile.good()) {
				cout << "Failed to open file ref.tcl" << endl;
				exit (1);
			}
			string total_list = get_depth_list();
			mFile << "set depth_list {\n";
			mFile << total_list;
			mFile << "}\n";
			mFile << "set trans_num "<<trans_num<<endl;
			mFile.close();
		}

		string get_depth_list () {
			stringstream total_list;
			total_list << "{mem_V " << mem_V_depth << "}\n";
			total_list << "{out_V " << out_V_depth << "}\n";
			total_list << "{w1_offset " << w1_offset_depth << "}\n";
			total_list << "{w2_offset " << w2_offset_depth << "}\n";
			total_list << "{input_offset " << input_offset_depth << "}\n";
			total_list << "{b1_offset " << b1_offset_depth << "}\n";
			total_list << "{b2_offset " << b2_offset_depth << "}\n";
			total_list << "{result_offset " << result_offset_depth << "}\n";
			return total_list.str();
		}

		void set_num (int num , int* class_num) {
			(*class_num) = (*class_num) > num ? (*class_num) : num;
		}
	public:
		int mem_V_depth;
		int out_V_depth;
		int w1_offset_depth;
		int w2_offset_depth;
		int input_offset_depth;
		int b1_offset_depth;
		int b2_offset_depth;
		int result_offset_depth;
		int trans_num;

	private:
		ofstream mFile;
		const char* mName;
};

extern void neural_network (
ap_int<8>* mem,
ap_int<32>* out,
unsigned int w1_offset,
unsigned int w2_offset,
unsigned int input_offset,
unsigned int b1_offset,
unsigned int b2_offset,
unsigned int result_offset);

void AESL_WRAP_neural_network (
ap_int<8>* mem,
ap_int<32>* out,
unsigned int w1_offset,
unsigned int w2_offset,
unsigned int input_offset,
unsigned int b1_offset,
unsigned int b2_offset,
unsigned int result_offset)
{
	refine_signal_handler();
	fstream wrapc_switch_file_token;
	wrapc_switch_file_token.open(".hls_cosim_wrapc_switch.log");
	int AESL_i;
	if (wrapc_switch_file_token.good())
	{
		CodeState = ENTER_WRAPC_PC;
		static unsigned AESL_transaction_pc = 0;
		string AESL_token;
		string AESL_num;
		static AESL_FILE_HANDLER aesl_fh;


		// output port post check: "out_V"
		aesl_fh.read(AUTOTB_TVOUT_PC_out_V, AESL_token); // [[transaction]]
		if (AESL_token != "[[transaction]]")
		{
			exit(1);
		}
		aesl_fh.read(AUTOTB_TVOUT_PC_out_V, AESL_num); // transaction number

		if (atoi(AESL_num.c_str()) == AESL_transaction_pc)
		{
			aesl_fh.read(AUTOTB_TVOUT_PC_out_V, AESL_token); // data

			sc_bv<32> *out_V_pc_buffer = new sc_bv<32>[10];
			int i = 0;

			while (AESL_token != "[[/transaction]]")
			{
				bool no_x = false;
				bool err = false;

				// search and replace 'X' with "0" from the 1st char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('X');
					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'out_V', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				no_x = false;

				// search and replace 'x' with "0" from the 3rd char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('x', 2);

					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'out_V', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				// push token into output port buffer
				if (AESL_token != "")
				{
					out_V_pc_buffer[i] = AESL_token.c_str();
					i++;
				}

				aesl_fh.read(AUTOTB_TVOUT_PC_out_V, AESL_token); // data or [[/transaction]]

				if (AESL_token == "[[[/runtime]]]" || aesl_fh.eof(AUTOTB_TVOUT_PC_out_V))
				{
					exit(1);
				}
			}

			// ***********************************
			if (i > 0)
			{
				// RTL Name: out_V
				{
					// bitslice(31, 0)
					// {
						// celement: out.V(31, 0)
						// {
							sc_lv<32>* out_V_lv0_0_9_1 = new sc_lv<32>[10];
						// }
					// }

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: out.V(31, 0)
						{
							// carray: (0) => (9) @ (1)
							for (int i_0 = 0; i_0 <= 9; i_0 += 1)
							{
								if (&(out[0]) != NULL) // check the null address if the c port is array or others
								{
									out_V_lv0_0_9_1[hls_map_index].range(31, 0) = sc_bv<32>(out_V_pc_buffer[hls_map_index].range(31, 0));
									hls_map_index++;
								}
							}
						}
					}

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: out.V(31, 0)
						{
							// carray: (0) => (9) @ (1)
							for (int i_0 = 0; i_0 <= 9; i_0 += 1)
							{
								// sub                    : i_0
								// ori_name               : out[i_0]
								// sub_1st_elem           : 0
								// ori_name_1st_elem      : out[0]
								// output_left_conversion : out[i_0]
								// output_type_conversion : (out_V_lv0_0_9_1[hls_map_index]).to_string(SC_BIN).c_str()
								if (&(out[0]) != NULL) // check the null address if the c port is array or others
								{
									out[i_0] = (out_V_lv0_0_9_1[hls_map_index]).to_string(SC_BIN).c_str();
									hls_map_index++;
								}
							}
						}
					}
				}
			}

			// release memory allocation
			delete [] out_V_pc_buffer;
		}

		AESL_transaction_pc++;
	}
	else
	{
		CodeState = ENTER_WRAPC;
		static unsigned AESL_transaction;

		static AESL_FILE_HANDLER aesl_fh;

		// "mem_V"
		char* tvin_mem_V = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_mem_V);

		// "out_V"
		char* tvin_out_V = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_out_V);
		char* tvout_out_V = new char[50];
		aesl_fh.touch(AUTOTB_TVOUT_out_V);

		// "w1_offset"
		char* tvin_w1_offset = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_w1_offset);

		// "w2_offset"
		char* tvin_w2_offset = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_w2_offset);

		// "input_offset"
		char* tvin_input_offset = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_input_offset);

		// "b1_offset"
		char* tvin_b1_offset = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_b1_offset);

		// "b2_offset"
		char* tvin_b2_offset = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_b2_offset);

		// "result_offset"
		char* tvin_result_offset = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_result_offset);

		CodeState = DUMP_INPUTS;
		static INTER_TCL_FILE tcl_file(INTER_TCL);
		int leading_zero;

		// [[transaction]]
		sprintf(tvin_mem_V, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_mem_V, tvin_mem_V);

		sc_bv<8>* mem_V_tvin_wrapc_buffer = new sc_bv<8>[8347050];

		// RTL Name: mem_V
		{
			// bitslice(7, 0)
			{
				int hls_map_index = 0;
				// celement: mem.V(7, 0)
				{
					// carray: (0) => (8347049) @ (1)
					for (int i_0 = 0; i_0 <= 8347049; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : mem[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : mem[0]
						// regulate_c_name       : mem_V
						// input_type_conversion : (mem[i_0]).to_string(2).c_str()
						if (&(mem[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<8> mem_V_tmp_mem;
							mem_V_tmp_mem = (mem[i_0]).to_string(2).c_str();
							mem_V_tvin_wrapc_buffer[hls_map_index].range(7, 0) = mem_V_tmp_mem.range(7, 0);
                                 	       hls_map_index++;
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 8347050; i++)
		{
			sprintf(tvin_mem_V, "%s\n", (mem_V_tvin_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_mem_V, tvin_mem_V);
		}

		tcl_file.set_num(8347050, &tcl_file.mem_V_depth);
		sprintf(tvin_mem_V, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_mem_V, tvin_mem_V);

		// release memory allocation
		delete [] mem_V_tvin_wrapc_buffer;

		// [[transaction]]
		sprintf(tvin_out_V, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_out_V, tvin_out_V);

		sc_bv<32>* out_V_tvin_wrapc_buffer = new sc_bv<32>[10];

		// RTL Name: out_V
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: out.V(31, 0)
				{
					// carray: (0) => (9) @ (1)
					for (int i_0 = 0; i_0 <= 9; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : out[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : out[0]
						// regulate_c_name       : out_V
						// input_type_conversion : (out[i_0]).to_string(2).c_str()
						if (&(out[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> out_V_tmp_mem;
							out_V_tmp_mem = (out[i_0]).to_string(2).c_str();
							out_V_tvin_wrapc_buffer[hls_map_index].range(31, 0) = out_V_tmp_mem.range(31, 0);
                                 	       hls_map_index++;
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 10; i++)
		{
			sprintf(tvin_out_V, "%s\n", (out_V_tvin_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_out_V, tvin_out_V);
		}

		tcl_file.set_num(10, &tcl_file.out_V_depth);
		sprintf(tvin_out_V, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_out_V, tvin_out_V);

		// release memory allocation
		delete [] out_V_tvin_wrapc_buffer;

		// [[transaction]]
		sprintf(tvin_w1_offset, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_w1_offset, tvin_w1_offset);

		sc_bv<32> w1_offset_tvin_wrapc_buffer;

		// RTL Name: w1_offset
		{
			// bitslice(31, 0)
			{
				// celement: w1_offset(31, 0)
				{
					// carray: (0) => (0) @ (0)
					{
						// sub                   : 
						// ori_name              : w1_offset
						// sub_1st_elem          : 
						// ori_name_1st_elem     : w1_offset
						// regulate_c_name       : w1_offset
						// input_type_conversion : w1_offset
						if (&(w1_offset) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> w1_offset_tmp_mem;
							w1_offset_tmp_mem = w1_offset;
							w1_offset_tvin_wrapc_buffer.range(31, 0) = w1_offset_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_w1_offset, "%s\n", (w1_offset_tvin_wrapc_buffer).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_w1_offset, tvin_w1_offset);
		}

		tcl_file.set_num(1, &tcl_file.w1_offset_depth);
		sprintf(tvin_w1_offset, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_w1_offset, tvin_w1_offset);

		// [[transaction]]
		sprintf(tvin_w2_offset, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_w2_offset, tvin_w2_offset);

		sc_bv<32> w2_offset_tvin_wrapc_buffer;

		// RTL Name: w2_offset
		{
			// bitslice(31, 0)
			{
				// celement: w2_offset(31, 0)
				{
					// carray: (0) => (0) @ (0)
					{
						// sub                   : 
						// ori_name              : w2_offset
						// sub_1st_elem          : 
						// ori_name_1st_elem     : w2_offset
						// regulate_c_name       : w2_offset
						// input_type_conversion : w2_offset
						if (&(w2_offset) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> w2_offset_tmp_mem;
							w2_offset_tmp_mem = w2_offset;
							w2_offset_tvin_wrapc_buffer.range(31, 0) = w2_offset_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_w2_offset, "%s\n", (w2_offset_tvin_wrapc_buffer).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_w2_offset, tvin_w2_offset);
		}

		tcl_file.set_num(1, &tcl_file.w2_offset_depth);
		sprintf(tvin_w2_offset, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_w2_offset, tvin_w2_offset);

		// [[transaction]]
		sprintf(tvin_input_offset, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_input_offset, tvin_input_offset);

		sc_bv<32> input_offset_tvin_wrapc_buffer;

		// RTL Name: input_offset
		{
			// bitslice(31, 0)
			{
				// celement: input_offset(31, 0)
				{
					// carray: (0) => (0) @ (0)
					{
						// sub                   : 
						// ori_name              : input_offset
						// sub_1st_elem          : 
						// ori_name_1st_elem     : input_offset
						// regulate_c_name       : input_offset
						// input_type_conversion : input_offset
						if (&(input_offset) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> input_offset_tmp_mem;
							input_offset_tmp_mem = input_offset;
							input_offset_tvin_wrapc_buffer.range(31, 0) = input_offset_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_input_offset, "%s\n", (input_offset_tvin_wrapc_buffer).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_input_offset, tvin_input_offset);
		}

		tcl_file.set_num(1, &tcl_file.input_offset_depth);
		sprintf(tvin_input_offset, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_input_offset, tvin_input_offset);

		// [[transaction]]
		sprintf(tvin_b1_offset, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_b1_offset, tvin_b1_offset);

		sc_bv<32> b1_offset_tvin_wrapc_buffer;

		// RTL Name: b1_offset
		{
			// bitslice(31, 0)
			{
				// celement: b1_offset(31, 0)
				{
					// carray: (0) => (0) @ (0)
					{
						// sub                   : 
						// ori_name              : b1_offset
						// sub_1st_elem          : 
						// ori_name_1st_elem     : b1_offset
						// regulate_c_name       : b1_offset
						// input_type_conversion : b1_offset
						if (&(b1_offset) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> b1_offset_tmp_mem;
							b1_offset_tmp_mem = b1_offset;
							b1_offset_tvin_wrapc_buffer.range(31, 0) = b1_offset_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_b1_offset, "%s\n", (b1_offset_tvin_wrapc_buffer).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_b1_offset, tvin_b1_offset);
		}

		tcl_file.set_num(1, &tcl_file.b1_offset_depth);
		sprintf(tvin_b1_offset, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_b1_offset, tvin_b1_offset);

		// [[transaction]]
		sprintf(tvin_b2_offset, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_b2_offset, tvin_b2_offset);

		sc_bv<32> b2_offset_tvin_wrapc_buffer;

		// RTL Name: b2_offset
		{
			// bitslice(31, 0)
			{
				// celement: b2_offset(31, 0)
				{
					// carray: (0) => (0) @ (0)
					{
						// sub                   : 
						// ori_name              : b2_offset
						// sub_1st_elem          : 
						// ori_name_1st_elem     : b2_offset
						// regulate_c_name       : b2_offset
						// input_type_conversion : b2_offset
						if (&(b2_offset) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> b2_offset_tmp_mem;
							b2_offset_tmp_mem = b2_offset;
							b2_offset_tvin_wrapc_buffer.range(31, 0) = b2_offset_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_b2_offset, "%s\n", (b2_offset_tvin_wrapc_buffer).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_b2_offset, tvin_b2_offset);
		}

		tcl_file.set_num(1, &tcl_file.b2_offset_depth);
		sprintf(tvin_b2_offset, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_b2_offset, tvin_b2_offset);

		// [[transaction]]
		sprintf(tvin_result_offset, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_result_offset, tvin_result_offset);

		sc_bv<32> result_offset_tvin_wrapc_buffer;

		// RTL Name: result_offset
		{
			// bitslice(31, 0)
			{
				// celement: result_offset(31, 0)
				{
					// carray: (0) => (0) @ (0)
					{
						// sub                   : 
						// ori_name              : result_offset
						// sub_1st_elem          : 
						// ori_name_1st_elem     : result_offset
						// regulate_c_name       : result_offset
						// input_type_conversion : result_offset
						if (&(result_offset) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> result_offset_tmp_mem;
							result_offset_tmp_mem = result_offset;
							result_offset_tvin_wrapc_buffer.range(31, 0) = result_offset_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_result_offset, "%s\n", (result_offset_tvin_wrapc_buffer).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_result_offset, tvin_result_offset);
		}

		tcl_file.set_num(1, &tcl_file.result_offset_depth);
		sprintf(tvin_result_offset, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_result_offset, tvin_result_offset);

// [call_c_dut] ---------->

		CodeState = CALL_C_DUT;
		neural_network(mem, out, w1_offset, w2_offset, input_offset, b1_offset, b2_offset, result_offset);

		CodeState = DUMP_OUTPUTS;

		// [[transaction]]
		sprintf(tvout_out_V, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVOUT_out_V, tvout_out_V);

		sc_bv<32>* out_V_tvout_wrapc_buffer = new sc_bv<32>[10];

		// RTL Name: out_V
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: out.V(31, 0)
				{
					// carray: (0) => (9) @ (1)
					for (int i_0 = 0; i_0 <= 9; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : out[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : out[0]
						// regulate_c_name       : out_V
						// input_type_conversion : (out[i_0]).to_string(2).c_str()
						if (&(out[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> out_V_tmp_mem;
							out_V_tmp_mem = (out[i_0]).to_string(2).c_str();
							out_V_tvout_wrapc_buffer[hls_map_index].range(31, 0) = out_V_tmp_mem.range(31, 0);
                                 	       hls_map_index++;
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 10; i++)
		{
			sprintf(tvout_out_V, "%s\n", (out_V_tvout_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVOUT_out_V, tvout_out_V);
		}

		tcl_file.set_num(10, &tcl_file.out_V_depth);
		sprintf(tvout_out_V, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVOUT_out_V, tvout_out_V);

		// release memory allocation
		delete [] out_V_tvout_wrapc_buffer;

		CodeState = DELETE_CHAR_BUFFERS;
		// release memory allocation: "mem_V"
		delete [] tvin_mem_V;
		// release memory allocation: "out_V"
		delete [] tvout_out_V;
		delete [] tvin_out_V;
		// release memory allocation: "w1_offset"
		delete [] tvin_w1_offset;
		// release memory allocation: "w2_offset"
		delete [] tvin_w2_offset;
		// release memory allocation: "input_offset"
		delete [] tvin_input_offset;
		// release memory allocation: "b1_offset"
		delete [] tvin_b1_offset;
		// release memory allocation: "b2_offset"
		delete [] tvin_b2_offset;
		// release memory allocation: "result_offset"
		delete [] tvin_result_offset;

		AESL_transaction++;

		tcl_file.set_num(AESL_transaction , &tcl_file.trans_num);
	}
}

